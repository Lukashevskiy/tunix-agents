# CrafText

Standalone package for the core `craftext` environment and dataset.

## Structure

- `craftext/` - Python package code
- `imgs/` - README assets
- `pyproject.toml` - package metadata and dependencies

## Install

```bash
pip install -e .
```

## Build

```bash
python -m build
```
